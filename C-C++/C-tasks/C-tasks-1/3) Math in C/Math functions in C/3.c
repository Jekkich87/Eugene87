#include <stdio.h>
#include <math.h>

int main() {
	
    double x1, y1, x2, y2, x_dif,y_dif, coord;
    
    scanf("%lf %lf %lf %lf",&x1, &y1, &x2, &y2);
    
    x_dif= pow((xa-xb),2);
    
    y_dif=pow((ya-yb),2);
    
    res=sqrt(x_dif+y_dif);
    
    printf("%.2lf",res); 
    
    return 0;
}
